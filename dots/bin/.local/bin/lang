#!/bin/bash
langfile=~/.lang
lang=`<$langfile`
case $lang in
	"us") lang="no" ;;
	*)		lang="us" ;;
esac
echo $lang > $langfile
setxkbmap $lang
xmodmap ~/.Xmodmap

for pid in `pgrep polybar`; do
	polybar-msg -p $pid hook lang 1;
done
