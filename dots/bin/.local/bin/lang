#!/bin/bash
langfile=~/.lang
lang=`<$langfile`
case $lang in
	"us")   lang="no" ;;
	*)      lang="us" ;;
esac
echo $lang > $langfile
setxkbmap $lang
xmodmap ~/.Xmodmap
polybar-msg action '#lang.hook.0'
