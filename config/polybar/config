;=====================================================
;     File Includes
;=====================================================

[section/base]
include-file = $HOME/.config/polybar/master.conf
include-file = $HOME/.config/polybar/modules.conf

[bar/i3]
inherit=bar/master
monitor =
monitor-fallback= 

tray-position = right
tray-padding = 1

modules-left = workspaces
modules-center = clock
modules-right = alsa backlight network battery

; Need to have backlight here, 
; didn't work when but in modules/
[module/backlight]
type = internal/backlight
card = intel_backlight
format = <label>
format-padding=1
format-prefix = " "
format-prefix-foreground = ${colors.bg}
format-background = ${colors.magenta-alt}
label = %percentage%%
label-foreground=${colors.bg}
